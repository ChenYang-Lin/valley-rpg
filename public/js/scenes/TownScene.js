import GameScene from "./GameScene.js";

export default class TownScene extends GameScene {
    constructor() {
        const name = "Town"
        super(name);
    }

    preload() {
        super.preload();
    }

    create() {
        super.create();

    }


    update(number, delta) {
        super.update(number, delta);
    }
}